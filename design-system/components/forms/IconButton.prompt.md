A square icon-only button using a Fluent System Icon glyph.

```jsx
<IconButton icon="/assets/icons/settings_20_regular.svg" label="Settings" variant="subtle" />
```

Always pass `label` for accessibility (used as `aria-label`/`title`). Variants mirror Button's primary/secondary/subtle.
